<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>添加账单 - 记账本</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 600px;
      margin: 0 auto;
    }

    .form-container {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 15px 50px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .form-header {
      text-align: center;
      margin-bottom: 32px;
    }

    .form-header h1 {
      font-size: 28px;
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 8px;
    }

    .form-header p {
      color: #718096;
      font-size: 14px;
    }

    .form-group {
      margin-bottom: 20px;
    }

    .form-group label {
      display: block;
      margin-bottom: 8px;
      font-weight: 500;
      color: #4a5568;
      font-size: 14px;
    }

    .form-control {
      width: 100%;
      padding: 14px 16px;
      border: 2px solid #e2e8f0;
      border-radius: 12px;
      font-size: 16px;
      transition: all 0.3s ease;
      background: #f7fafc;
    }

    .form-control:focus {
      outline: none;
      border-color: #667eea;
      background: white;
      box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
    }

    .form-control.date-picker {
      cursor: pointer;
    }

    select.form-control {
      cursor: pointer;
    }

    .form-row {
      display: flex;
      gap: 16px;
    }

    .form-row .form-group {
      flex: 1;
    }

    .btn {
      width: 100%;
      padding: 14px 24px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      border-radius: 12px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      margin-bottom: 16px;
    }

    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
    }

    .btn:active {
      transform: translateY(0);
    }

    .btn-secondary {
      background: rgba(0, 0, 0, 0.1);
      color: #666;
    }

    .btn-secondary:hover {
      background: rgba(0, 0, 0, 0.15);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    .type-toggle {
      display: flex;
      border-radius: 12px;
      overflow: hidden;
      border: 2px solid #e2e8f0;
      background: #f7fafc;
    }

    .type-toggle input[type="radio"] {
      display: none;
    }

    .type-toggle label {
      flex: 1;
      padding: 14px;
      text-align: center;
      cursor: pointer;
      transition: all 0.3s ease;
      font-weight: 500;
      margin: 0;
    }

    .type-toggle input[type="radio"]:checked + label {
      background: #667eea;
      color: white;
    }

    .type-toggle label:hover {
      background: rgba(102, 126, 234, 0.1);
    }

    .type-toggle input[type="radio"]:checked + label:hover {
      background: #5a67d8;
    }

    .back-link {
      text-align: center;
    }

    .back-link a {
      color: #667eea;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .back-link a:hover {
      color: #764ba2;
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .form-container {
        padding: 30px 20px;
        margin: 10px;
      }

      .form-row {
        flex-direction: column;
        gap: 0;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="form-container">
      <div class="form-header">
        <h1>💰 添加账单</h1>
        <p>记录您的每一笔收支，让理财更清晰</p>
      </div>

      <form method="post" action="/accounts/create">
        <div class="form-group">
          <label for="title">📝 事项</label>
          <input name="title" type="text" class="form-control" id="title" 
                 placeholder="请输入事项名称" required />
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="time">📅 时间</label>
            <input name="time" type="date" class="form-control date-picker" id="time" 
                   value="<%= new Date().toISOString().split('T')[0] %>" required />
          </div>

          <div class="form-group">
            <label for="account">💵 金额</label>
            <input name="account" type="number" class="form-control" id="account" 
                   placeholder="0.00" step="0.01" min="0" required />
          </div>
        </div>

        <div class="form-group">
          <label>📊 类型</label>
          <div class="type-toggle">
            <input type="radio" name="type" value="-1" id="expense" checked>
            <label for="expense">💸 支出</label>
            <input type="radio" name="type" value="1" id="income">
            <label for="income">💰 收入</label>
          </div>
        </div>

        <div class="form-group">
          <label for="remarks">📋 备注</label>
          <textarea name="remarks" class="form-control" id="remarks" rows="3"
                    placeholder="添加备注信息（可选）"></textarea>
        </div>

        <button type="submit" class="btn">
          ✨ 添加账单
        </button>

        <a href="/accounts" class="btn btn-secondary" style="text-decoration: none; text-align: center; display: block;">
          ← 返回列表
        </a>
      </form>

      <div class="back-link">
        <a href="/accounts">
          🏠 返回首页
        </a>
      </div>
    </div>
  </div>
</body>
</html>
