<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>记账本</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      color: #333;
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 20px;
    }

    .header {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 16px;
      padding: 24px;
      margin-bottom: 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
    }

    .header h1 {
      font-size: 28px;
      font-weight: 600;
      color: #2d3748;
      margin: 0;
    }

    .header-actions {
      display: flex;
      gap: 12px;
    }

    .btn {
      padding: 12px 24px;
      border: none;
      border-radius: 12px;
      font-size: 14px;
      font-weight: 500;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 8px;
      transition: all 0.3s ease;
      cursor: pointer;
    }

    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
    }

    .btn-secondary {
      background: rgba(0, 0, 0, 0.1);
      color: #666;
    }

    .btn-secondary:hover {
      background: rgba(0, 0, 0, 0.15);
      transform: translateY(-1px);
    }

    .account-list {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .account-item {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(10px);
      border-radius: 16px;
      padding: 20px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      transition: all 0.3s ease;
      border-left: 4px solid;
    }

    .account-item.expense {
      border-left-color: #e53e3e;
    }

    .account-item.income {
      border-left-color: #38a169;
    }

    .account-item:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.12);
    }

    .account-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 12px;
    }

    .account-date {
      font-size: 12px;
      color: #718096;
      font-weight: 500;
    }

    .account-type {
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 500;
    }

    .account-type.expense {
      background: rgba(229, 62, 62, 0.1);
      color: #e53e3e;
    }

    .account-type.income {
      background: rgba(56, 161, 105, 0.1);
      color: #38a169;
    }

    .account-body {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .account-title {
      font-size: 16px;
      font-weight: 500;
      color: #2d3748;
      flex: 1;
    }

    .account-amount {
      font-size: 18px;
      font-weight: 600;
      margin-right: 16px;
    }

    .account-amount.expense {
      color: #e53e3e;
    }

    .account-amount.income {
      color: #38a169;
    }

    .account-actions {
      display: flex;
      gap: 8px;
    }

    .btn-sm {
      padding: 8px 12px;
      font-size: 12px;
      border-radius: 8px;
      min-width: auto;
    }

    .btn-info {
      background: rgba(49, 130, 206, 0.1);
      color: #3182ce;
    }

    .btn-warning {
      background: rgba(237, 137, 54, 0.1);
      color: #ed8936;
    }

    .btn-danger {
      background: rgba(229, 62, 62, 0.1);
      color: #e53e3e;
    }

    .btn-sm:hover {
      transform: scale(1.05);
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: rgba(255, 255, 255, 0.8);
    }

    .empty-state h3 {
      font-size: 20px;
      margin-bottom: 8px;
    }

    .empty-state p {
      font-size: 14px;
      opacity: 0.7;
    }

    @media (max-width: 768px) {
      .header {
        flex-direction: column;
        gap: 16px;
        text-align: center;
      }

      .header-actions {
        width: 100%;
        justify-content: center;
      }

      .account-body {
        flex-direction: column;
        align-items: flex-start;
        gap: 12px;
      }

      .account-actions {
        width: 100%;
        justify-content: flex-end;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="header">
      <h1>💰 我的记账本</h1>
      <div class="header-actions">
        <a href="/accounts/create" class="btn btn-primary">
          ➕ 添加账单
        </a>
        <a href="/auth/logout" class="btn btn-secondary" onclick="return confirm('确定要退出登录吗？')">
          🚪 退出登录
        </a>
      </div>
    </div>

    <div class="account-list">
      <% if (accounts.length === 0) { %>
        <div class="empty-state">
          <h3>还没有账单记录</h3>
          <p>点击上方"添加账单"按钮开始记账吧！</p>
        </div>
      <% } else { %>
        <% accounts.forEach(item => { %>
          <div class="account-item <%= item.type === -1 ? 'expense' : 'income' %>">
            <div class="account-header">
              <div class="account-date">
                📅 <%= moment(item.time).format('YYYY年MM月DD日') %>
              </div>
              <div class="account-type <%= item.type === -1 ? 'expense' : 'income' %>">
                <%= item.type === -1 ? '💸 支出' : '💰 收入' %>
              </div>
            </div>
            <div class="account-body">
              <div class="account-title">
                <%= item.title %>
              </div>
              <div class="account-amount <%= item.type === -1 ? 'expense' : 'income' %>">
                ¥<%= item.account %>
              </div>
              <div class="account-actions">
                <a href="/accounts/view/<%= item._id %>" class="btn btn-sm btn-info" title="查看详情">
                  👁️
                </a>
                <a href="/accounts/edit/<%= item._id %>" class="btn btn-sm btn-warning" title="编辑">
                  ✏️
                </a>
                <a class="delBtn btn btn-sm btn-danger" href="/accounts/delete/<%= item._id %>" title="删除">
                  🗑️
                </a>
              </div>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>
  </div>

  <script>
    // 删除确认
    document.querySelectorAll('.delBtn').forEach(btn => {
      btn.addEventListener('click', function(e) {
        if (!confirm('确定要删除这条账单记录吗？')) {
          e.preventDefault();
        }
      });
    });
  </script>
</body>
</html>