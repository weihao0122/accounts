<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>账单详情 - 记账本</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      padding: 20px;
    }

    .container {
      max-width: 600px;
      margin: 0 auto;
    }

    .detail-container {
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(20px);
      border-radius: 20px;
      padding: 40px;
      box-shadow: 0 15px 50px rgba(0, 0, 0, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .detail-header {
      text-align: center;
      margin-bottom: 32px;
      padding: 20px;
      border-radius: 16px;
      border-left: 4px solid;
    }

    .detail-header.expense {
      background: rgba(229, 62, 62, 0.05);
      border-left-color: #e53e3e;
    }

    .detail-header.income {
      background: rgba(56, 161, 105, 0.05);
      border-left-color: #38a169;
    }

    .detail-header h1 {
      font-size: 24px;
      font-weight: 600;
      color: #2d3748;
      margin-bottom: 8px;
    }

    .detail-header .amount {
      font-size: 36px;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .detail-header .amount.expense {
      color: #e53e3e;
    }

    .detail-header .amount.income {
      color: #38a169;
    }

    .detail-type {
      display: inline-block;
      padding: 6px 16px;
      border-radius: 20px;
      font-size: 14px;
      font-weight: 500;
    }

    .detail-type.expense {
      background: rgba(229, 62, 62, 0.1);
      color: #e53e3e;
    }

    .detail-type.income {
      background: rgba(56, 161, 105, 0.1);
      color: #38a169;
    }

    .detail-info {
      display: grid;
      gap: 20px;
      margin-bottom: 32px;
    }

    .detail-item {
      padding: 20px;
      background: rgba(0, 0, 0, 0.02);
      border-radius: 12px;
      border: 1px solid rgba(0, 0, 0, 0.05);
    }

    .detail-label {
      font-size: 14px;
      color: #718096;
      font-weight: 500;
      margin-bottom: 8px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .detail-value {
      font-size: 16px;
      color: #2d3748;
      font-weight: 500;
      word-break: break-all;
    }

    .detail-id {
      font-family: 'Courier New', monospace;
      background: rgba(0, 0, 0, 0.05);
      padding: 8px 12px;
      border-radius: 8px;
      font-size: 12px;
      color: #4a5568;
    }

    .action-buttons {
      display: grid;
      grid-template-columns: 1fr 1fr 1fr;
      gap: 12px;
      margin-bottom: 20px;
    }

    .btn {
      padding: 14px 20px;
      border: none;
      border-radius: 12px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      text-decoration: none;
      text-align: center;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }

    .btn-secondary {
      background: rgba(0, 0, 0, 0.1);
      color: #666;
    }

    .btn-secondary:hover {
      background: rgba(0, 0, 0, 0.15);
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    }

    .btn-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
    }

    .btn-danger {
      background: rgba(229, 62, 62, 0.1);
      color: #e53e3e;
    }

    .btn-danger:hover {
      background: #e53e3e;
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(229, 62, 62, 0.4);
    }

    .back-link {
      text-align: center;
    }

    .back-link a {
      color: #667eea;
      text-decoration: none;
      font-weight: 500;
      transition: color 0.3s ease;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .back-link a:hover {
      color: #764ba2;
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .detail-container {
        padding: 30px 20px;
        margin: 10px;
      }

      .action-buttons {
        grid-template-columns: 1fr;
      }

      .detail-header .amount {
        font-size: 28px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="detail-container">
      <div class="detail-header <%= account.type === -1 ? 'expense' : 'income' %>">
        <h1><%= account.title %></h1>
        <div class="amount <%= account.type === -1 ? 'expense' : 'income' %>">
          <%= account.type === -1 ? '-' : '+' %>¥<%= account.account %>
        </div>
        <div class="detail-type <%= account.type === -1 ? 'expense' : 'income' %>">
          <%= account.type === -1 ? '💸 支出' : '💰 收入' %>
        </div>
      </div>

      <div class="detail-info">
        <div class="detail-item">
          <div class="detail-label">
            📅 记账时间
          </div>
          <div class="detail-value">
            <%= moment(account.time).format('YYYY年MM月DD日') %>
          </div>
        </div>

        <% if(account.remarks && account.remarks.trim()) { %>
        <div class="detail-item">
          <div class="detail-label">
            📋 备注信息
          </div>
          <div class="detail-value">
            <%= account.remarks %>
          </div>
        </div>
        <% } %>

        <div class="detail-item">
          <div class="detail-label">
            🔖 账单编号
          </div>
          <div class="detail-value">
            <div class="detail-id"><%= account._id %></div>
          </div>
        </div>
      </div>

      <div class="action-buttons">
        <a href="/accounts" class="btn btn-secondary">
          ← 返回
        </a>
        <a href="/accounts/edit/<%= account._id %>" class="btn btn-primary">
          ✏️ 编辑
        </a>
        <a href="/accounts/delete/<%= account._id %>" class="btn btn-danger delBtn">
          🗑️ 删除
        </a>
      </div>

      <div class="back-link">
        <a href="/accounts">
          🏠 返回首页
        </a>
      </div>
    </div>
  </div>

  <script>
    // 删除确认
    document.querySelector('.delBtn').addEventListener('click', function(e) {
      if (!confirm('确定要删除这个账单吗？删除后无法恢复！')) {
        e.preventDefault();
      }
    });
  </script>
</body>
</html> 